<!-- tingle:常驻件-version 1 -->

## 这个文件夹是一个 tingle 客户项目

本文件夹是某个客户项目在这台机器上的工作目录。项目的共享事实（资料、上下文、各处地址）在飞书上只有一份，本机只存一个指向它的小文件 `.tingle/state.json`。

**开工前先跑一次检查。** 每个技能运行前都要跑 `tingle check <技能名>`：拿到开工单直接干，拿到卡点单先修、修完复验。检查每次现跑，不存结论——授权几小时就过期、表会被挪走，存下来的结论坏了无声。

**程序判机械事实，你判内容。** 程序只查「东西在不在、打不打得开、版本对不对、登记齐不齐」。声明里那些合格条件（这份东西要满足什么才算能用）程序一概不验——**由你在真正开跑时逐条对照判**。判不准就停下问人，不要挑一个解释混过去。

**对外写入先摆清单等放行。** 凡要写进飞书的动作，先把「要写什么、写到哪」摆出来，等人点头再落笔。

**材料里没有的不编造。** 取不到的值标出来问，不拿品类常识填空。

<!-- 本段内容尚未定稿：公理、姿态、判断记录写法等待「操盘手思维大脑」那条线产出后填入。
     模版住插件仓、随发版更新；本机实例照它重建，标记区外一字不动。 -->
