---
name: context-distill
description: >-
  何时用：一个项目手上有一堆散在各处（在线文档 / 多维表格 / 电子表格 / 会议妙记 / 云盘 / 本地）的
  原始材料，需要把它们蒸馏成一套「每条都能溯回原材料」的结构化认知件（多件 + 一份索引）、供下游程序反复引用时使用。
  典型场景：新接一个项目、材料零散待整理成一套可复用的项目上下文认知件；或已有这套产物、某处材料更新了
  要就地重蒸。可检索关键词：项目材料蒸馏、上下文蒸馏、context distill、逐条溯源、结构化产物、
  材料归集、缺口标注、raw 材料整理、溯源产物。前提：一次只处理一个项目、其材料可读取（飞书优先）。
  不用于：给项目建档 / 建库 / 配权限 / 通知团队（组织层的活，不在本环节）；也不做下游的选题 /
  审稿 / 内容 / 投放 / 分析（这些是蒸馏产物的独立消费者，各自是独立环节）。
---

# context-distill

## 概述与核心原则

本 skill 只做一件事：**把一个项目散在各处的原始材料，经「引导询问 → 取料归集 → 蒸馏 → 查缺 → 索取 → 重蒸」的循环，蒸成一套每条都能溯回原材料的结构化认知件——每件独立成飞书文档、各落各自的文件夹，外加一份索引串起整套，摊在项目云盘顶层，供下游程序反复引用；材料更新就地重蒸。**

**三条核心原则（记牢，越一条即错）：**

- **material-first —— 从真实材料起步，蓝图只事后核。** 循环第一步是引导人报出手上有什么料，产物结构从真实材料长出。任何"该有哪些块"的蓝图 / 下游契约，只在蒸馏之后的"查缺"步当**事后核对表**，绝不反过来当上游驱动去照单收料、先搭空框。数据只单向流：真实料 → 蒸馏 → 事后拿蓝图核缺。
- **每条溯源 + 不发明。** 产物里每条实质内容都挂一个具名的一手来源，第三人拿源 + 这条产物能逐条比对。材料对不上的**标"材料不足待补"**，绝不凭品类常识 / 泛化硬编填空。
- **产出者不自宣完成。** 蒸馏产物是待复核件，由一个独立评审者拿产物级标准逐条判 PASS/FAIL 后才升级；skill 自己不拍板"做完了"。

**适用环节**：本 skill 对应一条可复用环节——把一个项目的散乱 raw 材料整理成一套溯源认知件，跨项目复现（每个项目可换、材料更新可重跑），不是一次性事务。

## 运行前检查与输入声明

**Step 0 · 运行前检查（仅此一行，不自带判定）**：REQUIRED SUB-SKILL: `tingle:project-readiness`
——传入本 skill 名；返回「放行」才继续运行，并以其返回的已解析地址为各输入的取用地址；
返回「不就绪」则本次不运行，待其补齐并复验通过后重来。

**唯一例外 · 由 project-readiness 编排发起的初蒸调用**：此时项目档案与本地锚尚未建成（建档案是初始化的最后一道），本检查**不触发**——运行前提已由编排方在本次初始化中承担，入参（项目云盘 `folder_token` 等）由编排方直传，见下方入参契约。除此之外的一切入径（使用者直呼、重蒸、修复路由）均照常先跑本检查。

本 skill 的 requirements 声明如下——只声明输入的类型（`type`）与内容级就绪判据，不含任何具体地址值；输入的实际地址以上述运行前检查返回的已解析地址为准（**初蒸例外**：检查不触发，地址由编排方直传，见入参契约），就绪判据由该检查当场核验。

```json
{
  "requirements": {
    "项目云盘": {
      "type": "lark-drive",
      "就绪判据": ["文件夹可访问"]
    }
  }
}
```

## 何时用 / 何时不用

**何时用**
- 新接一个项目，材料零散在在线文档 / 多维表格 / 电子表格 / 妙记 / 云盘 / 本地各处，要整理成一套可复用、每条溯源的认知件（含索引）。
- 已有这套产物，某处材料更新了 / 某环节定稿产物要纳入，要就地重蒸、受影响的件就地更新。
- 需要一个"一个项目 raw → 溯源认知件套"的通用蒸馏引擎，换个项目零改复用。

**何时不用（When NOT）**
- **给项目建档 / 建库 / 配权限 / 通知团队** —— 这些是组织层的活，不在本环节。本 skill 只读别人给的标签 / brief（有则读，无则从材料认），不建档。
- **做下游的选题 / 审稿 / 内容 / 投放 / 分析** —— 这些是蒸馏产物的独立消费者、各自独立环节。本 skill 只交"溯源产物"，越到任一件即错。
- **"任意材料 → 任意产物"的泛化引擎** —— 本 skill 锁定"一个项目 raw → 一套溯源认知件"这一类，不做通用文本转换器。
- **判断"现在该不该蒸、该跑哪个环节 / 哪个 skill"（平台路由）** —— 本 skill 无平台判断脑，每次调用自带意图；环节编排归调用方（如 `project-readiness` 的初始化编排）。
- **材料完全无法读取时硬跑** —— 读不到就**停并报缺**（缺哪份、在哪），不猜路径、不凭常识硬编一份出来。

## 方法：material-first 六步循环

一次调用 = 一个反馈环：收料与蒸馏紧耦合（蒸出来发现缺 → 再收 → 再蒸），不拆成两段。

> **material-first 硬约束（贯穿全环，先记死）**：循环首步是"引导人报出手上有什么料"、从真实材料起步。查缺蓝图 / 下游契约**只在第 4 步"查缺"**作为事后 checklist 出现。全流程**没有**"先照蓝图算出需要哪些字段 → 照单收料 / 先搭空产物骨架再填"的上游驱动步——那是本 skill 明令排除的反模式。

### 调用意图与入参契约

**本 skill 无平台判断脑**——不判断"现在该不该蒸、该跑哪个环节"；每次调用自带意图，三类：

- **(a) 初蒸**：由 `project-readiness` 在项目初始化"开云盘落点 → 收料 → 蒸馏"的编排处**一次性合并调用**，走完整六步循环（引导收料 + 上飞书 + 蒸馏一体）；调用方不参与过程、只验收结果（云盘有料 / 上下文产出 / 取地址登记）。
- **(b) 重产某件 / 某投影**：消费 skill 运行前检查发现某投影不达标 → 按 `produced_by: context-distill` 的修复路由回调本 skill；料够则重蒸该件，料不足则先查缺 / 索取再蒸。
- **(c) 收新料 / 定稿纳入 / 料更新重蒸**：项目成员直达本 skill——材料的事直达，不必绕编排；**环节定稿产物一旦产生，必纳回这一整套（定稿必纳入）**，随后重蒸受影响的件。

**入参契约**：
- **必传 = 项目云盘 `folder_token`**。初蒸时由调用方直传（**勿假设项目档案已有本 skill 的条目**）；重蒸 / 修复路由时由调用方从项目档案解析后传入。
- **可选** = 已知来料线索（未传则第 1 步引导询问现场产出）、成员名单（认知件套的成员定义）。
- （可选）**查缺蓝图链接**（如组织维护的物料清单库）+ **下游契约链接集**（如某下游 skill 的 input 字段声明）——**仅第 4 步查缺当事后 checklist 用，运行时注入、不硬编**。

**本 skill 保留的判断只有材料域的**：这批料影响哪些件、缺哪块料该向谁要。环节编排、阶段路由归调用方。

**读不到料即停**：某份线索指向的材料读不到，停并报 `缺某材料，读不到，请确认位置 / 权限`，不猜路径、不硬编。

### 依赖

**依赖的规则层与项目事实**：
- **不装配品类 / 品牌等创作规则层**——蒸馏是"忠实搬运 + 溯源"，不套创作 / 合规规则去改写内容；材料里带的口径 / 限定块随条目沿用（从材料来，不从规则层来）。
- **运行时注入项**（不内嵌进本 skill 正文）：调用意图与项目云盘 `folder_token`（见上）；来料线索清单（人报或引导产出）；成员名单（认知件套的成员定义）；可选的查缺蓝图链接、下游契约链接集（第 4 步用）；项目档案的标签 / 阶段值（「tingle-项目档案」他方建维，本 skill **不建不写、只读**）。换个组织 / 无蓝图时，skill 仍可只用下游契约、或跳过查缺、仅靠纪律"缺则标待补"兜底。

**依赖的工具 / 接口**（读散在各处的项目料，飞书优先）：
- `lark-base`（多维表格）、`lark-sheets`（电子表格）、`lark-doc`（云文档 / Wiki 文档）、`lark-minutes`（妙记）——只读项目料；
- `lark-drive`（云盘文件夹 / 上传下载 / 快捷方式 / 本地件导入）——**建云盘文件夹 / 写飞书 = 对外不可逆动作，先放行再执行**；
- 本地 `Read`、PDF 走 `pdf2markdown` / `pdf`——只用于转存前识读：**本地料 = 转存过渡态**，先上传项目云盘 `raw/` 并转成相应格式、然后才处理，材料不滞留对话、不滞留本地。

四类飞书资源 → 四 lark skill 的具名映射、云盘操作、本地料转存规则，详见 `references/landing-and-read-contract.md` 「飞书读取契约」一节。

### 输入 / 输出 schema

- **input schema** = `{ 调用意图（初蒸 | 重产某件·某投影 | 收新料·定稿纳入·料更新重蒸）；项目云盘 folder_token（必传）；可选：来料线索清单[每条：材料名 / 类型（多维表格|电子表格|文档|妙记|云盘文件|本地文件）/ 位置 / 疑似一手 or 加工件]、成员名单、查缺蓝图链接、下游契约链接集、重产目标（重产意图时必带） }`。
- **output schema** = `{ 认知件套：成员由运行时注入的清单（成员名单 / 蓝图 / 下游需求）定义、不硬编，每件独立成飞书文档、各落各自的文件夹，件内[按主题分块 + 每条溯源标注（具名一手源）+ 可选口径承接 / 限定块 + 缺口标注 + 文末溯源附表]；索引：一份、串起整套，每件一条（件名 + 链接 + 状态 / 缺口）；下游投影：独立文档、同为套内成员 }；落点：{ 各认知件（各自文件夹）；索引；raw/；raw/索引.md }`。

产物 schema **不固定**（因项目 / 下游而变），故上面声明的是**结构契约**而非固定字段值。字段级判据（每字段类型 / 格式 / 判据 / 占位符正例）见 `references/landing-and-read-contract.md` 「input / output schema 字段级判据」一节。

### 六步

**Step 1 · 引导询问**
- skill **主动引导**人报出：有没有材料？有哪些文件？在哪（在线文档 / 多维表格 / 电子表格 / 妙记 / 云盘 / 本地各处）？**不假设自己知道去哪读**，一项项问清。
- 出：一张"有哪些料、各在哪、疑似一手还是加工件"的**来料线索清单**（一次性工作件，归集后即弃，不入云盘明面）。

**Step 2 · 取料归集**
- 按线索用读取契约（见 reference）读取，归集进项目云盘 `raw/`：内部按来源簇归类、三层去重、一手 vs 加工件分层。
- 在线原生件（妙记 / 在线文档 / 多维表格）**不搬原件**——在 `raw/索引.md` 登记一行（标题 + 类型 + 飞书链接 / token + 一句摘要 + 读它用哪个 lark skill），必要处加飞书快捷方式指过去。
- 落点结构、去重三层、在线件登记法，详见 `references/landing-and-read-contract.md` 「落点结构」一节。

**Step 3 · 蒸馏**
- 调 `references/distill-craft.md` 的**四步手艺**（并行只读抽取 → 装配忠实台账 → 逐格溯源蒸馏 → 独立评审），把 `raw/` 蒸成**认知件套**——每件独立成飞书文档、各落各自的文件夹、摊在云盘顶层，并维护一份**索引**串起整套（每件一条：件名 + 链接 + 状态 / 缺口）。套成员由运行时注入的清单（成员名单 / 蓝图 / 下游需求）定义，**不硬编成员**；下游投影同为独立文档、同为套内成员。
- 对不上料的块标"材料不足待补"；受监管 / 有口径风险的领域，把口径承接 / 限定块随条目一并写上。
- 工序脚手架（extract 束 + 忠实台账）落 `raw/_台账/`（非引用面）。

**Step 4 · 查缺**
- 拿运行时注入的查缺蓝图 + 下游契约当**事后 checklist**，核对"该有的件 / 块漏没漏"。
- **阶段感知过滤**：checklist 按 `{阶段 × 项目类型}` 过滤——**本阶段不该有的缺项不算缺口、不去催**（如策略期缺执行数据属"本阶段不该有"）。阶段值读项目档案「阶段」字段（受控初始集：策略期 / 执行中 / 新周期，可扩）；初蒸时标签（品类三级 `{业务线 / 合规类目 / 产品品类}` + 品牌）/ 阶段尚未落定 → 询问用户或从材料认，不发明。**阶段值只用于材料域查缺，不做"哪个阶段跑哪些 skill"的平台路由。**
- **只事后校验，绝不照蓝图算需求再收料**（material-first 护栏见 reference）。蓝图是 type-level 通用清单，只回答"这类产物一般该有哪些种类的块"，绝不回答"某项目某块内容是什么"——内容只能从真实 raw 蒸。
- 出：一张缺口清单（一次性工作件，索取后即弃）。

**Step 5 · 索取**
- 把真缺的列成缺口清单去向人要；人给了新料，**回第 2 步**归集进 `raw/`（+ 索引）。

**Step 6 · 重蒸**
- 料补齐 / 更新 → **回第 3 步就地重跑**；受影响的认知件就地更新、索引同步，落点 / raw 结构不重建。

## 决策档 · 复核包 · 升级条件

- **决策档 = 复核**。本 skill 产出的是**待复核蒸馏产物**，由人验收后才升级；skill 不自宣完成、不替人拍板。
- **复核包（交人复核时随附）**：① 本次产出 / 更新的认知件（含索引）；② 溯源可核（每条挂具名一手源、可逐条回核）；③ 缺口清单（哪些块料不足 / 待核 / 可确无）；④ 台账素材警示（蒸馏前钉死的雷区，供核是否被绕开）；⑤ **独立评审判定记录**（拿产物级标准逐条 PASS/FAIL 的结果——这是复核过程件，不是产物、不入 `raw/`）。
- **升级条件（命中任一即停、交人裁决，不自行推进）**：
  1. **合规 / 口径红线不确定** —— 某条内容是否越线、口径怎么承接判不准；
  2. **一手源内部冲突** —— 多份一手材料对同一事实互相打架、无法自洽取舍；
  3. **缺关键材料** —— 缺某份料导致某必备块蒸不出，非补料不能推进；
  4. **对外不可逆动作** —— 建云盘落点文件夹 / 写飞书 / 发送到项目外部等，**先放行再执行**。**一次放行覆盖本次运行在该项目云盘内的建目录与写入**（含 `raw/_台账/`、各认知件与索引的建立及就地更新），不逐次重复请示；跨项目动作、删除类动作另行请示。

## 写回位置 · 可沉淀候选

- **写回位置** = 项目云盘：顶层摊认知件套 + 索引（下游引用面；成员由运行时清单定义）、`raw/` 收原料（溯源回查面）。落点结构详见 `references/landing-and-read-contract.md` 「落点结构」一节。
- **可沉淀候选**（output schema 中标"是"）：认知件中经复核确认、**跨材料合成且稳定**的件 / 块（如项目身份 / 人群卡点 / 认知主线类）可标为回流资产、供复用；纯溯源脚手架（extract / 台账 / raw 索引）标"否"、不回流。

## 产出验收判据（DoD）

一套蒸馏产物（认知件套）**合格**，当且仅当以下全部成立（可判句、非裸形容词）：
1. **该有的件 / 块齐** —— 按 `{阶段 × 项目类型}` 过滤后的查缺无遗漏，或遗漏已显式标"材料不足待补"；
2. **每条溯源可核** —— 每条实质内容挂具名一手源，第三人可逐条回核到原材料段落；
3. **溯源抽查无发明** —— 抽查任意若干条，均能落回具名源，无凭常识 / 泛化硬编的定论；
4. **缺口显式标注** —— 料没覆盖的地方标"待补 / 待核 / 可确无·以某代理指标"，不静默填充；
5. **素材雷区被绕开** —— 台账素材警示列出的判断陷阱，逐一被产物绕开（无被禁话术、无归属存疑卖点当定论、无截断数据当全量）；
6. **独立评审逐条 PASS** —— 独立评审者拿产物级标准逐条判，全 PASS（产出者不自宣）；
7. **索引与套一致** —— 每件在索引有一条（件名 + 链接可达、状态如实），无孤儿件、无悬空条目。
> 内容层面的具体对错（哪些口径越线）交由运行时项目材料 + 可选下游契约承载；本判据本身可据以判定某份产物合格与否。

## Quick Reference

| 环节 | 一句话 |
|---|---|
| 调用意图 | 三类自带：初蒸（`project-readiness` 初始化编排一次性合并调用）/ 重产某件·某投影（`produced_by` 修复路由回调）/ 收新料·定稿纳入·料更新重蒸（成员直达；定稿必纳入） |
| 入参 | 必传 = 项目云盘 `folder_token`（初蒸直传、勿假设项目档案已有条目；重蒸从项目档案解析）；可选 = 来料线索、成员名单、蓝图 / 下游契约链接；读不到料即停报缺 |
| Step 1 引导询问 | 主动问人：有哪些料、各在哪、疑似一手还是加工件；不假设自己知道去哪读 |
| Step 2 取料归集 | 按读取契约读 → 归集进 `raw/`（归类 / 去重 / 一手分层）；在线原生件不搬原件、`raw/索引.md` 登记；本地料 = 转存过渡态先上云盘 |
| Step 3 蒸馏 | 四步手艺（并行抽取→装配台账→逐格溯源蒸馏→独立评审）→ 认知件套（每件独立 doc、各自文件夹）+ 索引；脚手架落 `raw/_台账/` |
| Step 4 查缺 | 拿蓝图 / 下游契约当**事后** checklist、按 `{阶段 × 项目类型}` 过滤（本阶段不该有的不算缺口）；绝不照蓝图上游驱动收料 |
| Step 5 索取 | 真缺的列清单去向人要；补来的回 Step 2 归集 |
| Step 6 重蒸 | 料更新 → 回 Step 3 就地重跑；受影响件就地更新、索引同步、结构不重建 |
| 读取契约 | 多维表格→lark-base / 电子表格→lark-sheets / 文档→lark-doc / 妙记→lark-minutes；云盘→lark-drive；本地料 = 转存过渡态（先上云盘转格式再处理） |
| 落点 | 顶层 = 认知件套 + 索引（引用面）；「tingle-项目档案」他方建维、本 skill 只读；`raw/` = 原料（回查面）；脚手架→`raw/_台账/` |
| 决策档 | 复核（产待复核产物，人验收才升级） |
| 必停升级 | 合规口径不确定 / 一手源冲突 / 缺关键材料 / 对外不可逆动作（先放行） |
| 可沉淀 | 跨材料合成且稳定的块（复核后回流）；脚手架不回流 |
| 不做 | 建档 / 建库 / 权限 / 通知；下游选题 / 审稿 / 内容 / 投放 / 分析 |

## 常见错误（Common Mistakes）

- **把蓝图当上游驱动照单收料** —— 先照 checklist 算出应有字段、再去找料 / 先搭空框。违 material-first。蓝图只在蒸馏之后当事后核对表。
- **对不上料硬编泛化填空** —— "这块没料，凭品类常识补一句吧"。违不发明。没源就标"材料不足待补"。
- **溯源指向脚手架而非一手** —— 来源标注指向 extract / 台账等中间件。违每条溯源。来源须指真实一手材料段落。
- **把前人加工件当一手 / 当"已满足"** —— 别的目的裁剪组织过的加工件，直接采信为承重。违回一手。加工件只作参考待核，要用回它对应的一手原件重做。
- **产物留内部裸码 / 流程标签** —— 正文留"品-1"类裸码、"承重"类流程标签、指向脚手架的来源。违产物自解释。内部代号 / 映射一律隔离进文末溯源附表。
- **建云盘 / 写飞书不先请示** —— 直接建文件夹、直接写飞书表。违对外动作先放行。命中升级条件，先摆出要执行、等放行。
- **产出者自宣完成** —— 蒸完自己说"合格"。违产出者不自宣。须独立评审逐条判 PASS。
- **初蒸时去项目档案找本 skill 的条目** —— 初蒸时项目档案尚无本 skill 条目，`folder_token` 由调用方直传；去解析等于对不存在的登记硬跑。
- **把"本阶段不该有"的缺项当缺口去催料** —— 查缺先按 `{阶段 × 项目类型}` 过滤；策略期不催执行数据。
- **对话里贴来的材料直接蒸** —— 本地料 = 转存过渡态：先上传项目云盘 `raw/` 转格式，再按云盘件处理；材料不滞留对话 / 本地。

## Red Flags

看到自己出现以下念头或动作，停下重判：
- 「先照 checklist 把该有的框搭全，再去找料填」→ 上游驱动，违 material-first，收回。
- 「这块没料，凭品类常识补一句吧」→ 发明，标"材料不足待补"，别硬编。
- 「这份前人加工过的，直接当一手用吧」→ 违回一手，回它的一手原件重做。
- 「来源就写指向那份 extract 吧」→ 溯源指脚手架，改指真实一手材料。
- 「直接建云盘文件夹 / 改飞书吧」→ 对外不可逆动作，先请示放行。
- 「我蒸完了，应该没问题，交付吧」→ 没独立评审，别自宣，先派独立评审逐条判。
- 「用户贴在对话里的料，顺手直接蒸吧」→ 先上云盘 `raw/` 转格式再处理，不滞留对话。
- 「这个缺口先催了再说」→ 先核阶段：本阶段不该有的缺项不算缺口、不去催。
